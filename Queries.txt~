** CHECK EVERY QUERY

Query 1:

SELECT STNAME FROM HU_UNIT_STATE_LEVEL WHERE HUEST_2011 > (SELECT HUEST_2010 FROM HU_UNIT_STATE_LEVEL WHERE STNAME = "Alabama");


Query 2:

SELECT COUNT(*) AS StateCount, R.REGION_Desc FROM POP_EST_NATION_STATE_PR N, REGION R WHERE N.REGION = R.REGION_Cd GROUP BY R.REGION_Cd;

Query 3:

SELECT D.NAME, D.CENSUS2010POP, H.HUEST_2010 FROM HU_UNIT_STATE_LEVEL H, POP_EST_NATION_STATE_PR D WHERE D.NAME = H.STNAME GROUP BY D.NAME;

Query 4:

SELECT CTYNAME, MAX(POPESTIMATE2012) FROM POP_EST_STATE_COUNTY;

Query 5:

??


Query 6:

SELECT NAME FROM POP_EST_NATION_STATE_PR WHERE BIRTHS2010 > DEATHS2010 ORDER BY REGION ASC, BIRTHS2010 DESC;



Query 7:

??



